import React // Creating a component for creating space between text. Used e.g. in the About-section.
    from 'react';
import {
    View,
} from 'react-native';

const creatingSpace =
        <View style={{ height: 20 }}>
        </View>

export { creatingSpace };